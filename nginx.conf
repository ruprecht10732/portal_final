server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  gzip on;
  gzip_min_length 1024;
  gzip_proxied any;
  gzip_types
    text/plain
    text/css
    text/xml
    application/json
    application/javascript
    application/xml
    application/rss+xml
    image/svg+xml;

  location = /healthz {
    access_log off;
    default_type text/plain;
    return 200 "ok";
  }

  # App is mounted under /klant in the reverse proxy.
  # Rewrite /klant/* to / so built assets (main-*.js, chunk-*.js, etc.) resolve.
  location = /klant {
    return 301 /klant/;
  }

  location / {
    rewrite ^/klant/(.*)$ /$1 break;
    try_files $uri $uri/ /index.html;
  }

  location /assets/ {
    try_files $uri =404;
    access_log off;
    expires 1y;
    add_header Cache-Control "public, max-age=31536000, immutable";
  }

  location ~* \.(?:js|css|json|ico|png|jpg|jpeg|gif|svg|webp|woff2?)$ {
    rewrite ^/klant/(.*)$ /$1 break;
    try_files $uri =404;
    access_log off;
    expires 1y;
    add_header Cache-Control "public, max-age=31536000, immutable";
  }
}
